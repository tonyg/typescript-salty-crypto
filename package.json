{
  "name": "salty-crypto",
  "version": "0.0.0",
  "scripts": {
    "compile": "yarn tsc",
    "compile:watch": "yarn compile -w",
    "test": "yarn jest",
    "test:watch": "yarn test --watch",
    "clean": "rm -rf lib/"
  },
  "devDependencies": {
    "@types/jest": "^29.2.6",
    "esm": "^3.2.25",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.5",
    "typescript": "4.9"
  },
  "dependencies": {
  }
}
